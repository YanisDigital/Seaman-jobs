# Зависимости облачной веб-версии (Streamlit Cloud ставит ИМЕННО этот файл).
# Лёгкие, без браузера: облачная версия работает с crewell + ukrcrewing.
# Для десктопа/.exe (с maritime-zone) — см. requirements-desktop.txt
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
openpyxl>=3.1
PyYAML>=6.0
streamlit>=1.30
